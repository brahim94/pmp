<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="pmp_report.list">
      <field name="name">pmp_report list</field>
      <field name="model">pmp_report.pmp_report</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="pmp_report.action_window">
      <field name="name">pmp_report window</field>
      <field name="res_model">pmp_report.pmp_report</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="pmp_report.action_server">
      <field name="name">pmp_report server</field>
      <field name="model_id" ref="model_pmp_report_pmp_report"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="pmp_report" id="pmp_report.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="pmp_report.menu_1" parent="pmp_report.menu_root"/>
    <menuitem name="Menu 2" id="pmp_report.menu_2" parent="pmp_report.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="pmp_report.menu_1_list" parent="pmp_report.menu_1"
              action="pmp_report.action_window"/>
    <menuitem name="Server to list" id="pmp_report" parent="pmp_report.menu_2"
              action="pmp_report.action_server"/>
-->
  </data>
</odoo>